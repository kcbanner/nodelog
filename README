Dependencies:

See package.json. Install the latest version of mongodb native driver from git, as 0.8.0 isn't available via npm yet.

Get datejs from my repo: https://github.com/kcbanner/datejs it includes the 'S' format specifier, the original version does not.