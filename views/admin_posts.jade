article  
  a(href='/admin') Back
  a(href='/admin/post/new') New

  table.admin
    thead
      tr
        th Title
        th Date
        th Published
        th 
    tbody
      - each post in posts
        tr
          td
            a(href='/admin/post/edit/'+post.id)= post.title
          td= post.date.format('MMMM, dd, yyyy')
          td
            a(href='/admin/post/publish/'+post.id)
              - if(post.published)
                | Yes
              - else 
                | No
          td
            a(href='/admin/post/delete/'+post.id) Delete
